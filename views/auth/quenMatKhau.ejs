<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Quên Mật Khẩu</title>
    <style>
        /* Your existing CSS remains unchanged */
        * {
            box-sizing: border-box;
        }

        .message-success {
            color: #2ecc71;
            font-weight: bold;
        }

        .message-error {
            color: #e74c3c;
            font-weight: bold;
        }

        body {
            margin: 0;
            background: linear-gradient(rgba(120, 157, 183, 0.9), rgba(94, 101, 105, 0.9)),
                url('https://images.unsplash.com/photo-1577896851231-70ef18881754?ixlib=rb-4.0.3&auto=format&fit=crop&w=1350&q=80') no-repeat center center fixed;
            background-size: cover;
            font-family: 'Arial', sans-serif;
            display: flex;
            justify-content: center;
            align-items: center;
            flex-direction: column;
            height: 100vh;
            color: #2c3e50;
        }

        .logo-container {
            width: 100px;
            height: 100px;
            margin-bottom: 20px;
        }

        .logo-container img {
            width: 100%;
            height: 100%;
            object-fit: contain;
            filter: drop-shadow(0 4px 10px rgba(0, 0, 0, 0.2));
        }

        .login-wrapper {
            background: rgba(255, 255, 255, 0.95);
            max-width: 500px;
            width: 90vw;
            padding: 32px 28px 40px 28px;
            border-radius: 20px;
            box-shadow: 0 10px 20px rgba(0, 0, 0, 0.2);
            text-align: center;
            position: relative;
        }

        .decor-circle {
            position: absolute;
            border-radius: 50%;
            opacity: 0.2;
        }

        .decor-circle.one {
            width: 100px;
            height: 100px;
            background: #3498db;
            top: -30px;
            left: -30px;
        }

        .decor-circle.two {
            width: 70px;
            height: 70px;
            background: #5dade2;
            bottom: -25px;
            right: -25px;
        }

        h1 {
            font-weight: 700;
            margin-bottom: 24px;
            font-size: 2.2rem;
            color: #2c3e50;
        }

        form {
            text-align: left;
        }

        label {
            font-weight: 600;
            font-size: 0.95rem;
            color: #2c3e50;
            margin-bottom: 6px;
            display: inline-block;
        }

        input[type="email"] {
            width: 100%;
            padding: 14px 18px;
            border-radius: 20px;
            border: 2px solid #3498db;
            font-size: 1rem;
            font-family: 'Arial', sans-serif;
            color: #2c3e50;
            margin-bottom: 22px;
            transition: border-color 0.3s ease, box-shadow 0.3s ease;
            box-shadow: inset 1px 1px 6px #d6eaf8;
        }

        input[type="email"]::placeholder {
            color: #7f8c8d;
            font-style: italic;
        }

        input[type="email"]:focus {
            outline: none;
            border-color: #2980b9;
            box-shadow: 0 0 8px rgba(41, 128, 185, 0.5);
            background-color: #f5f9ff;
        }

        button {
            width: 100%;
            padding: 14px 0;
            background: linear-gradient(45deg, #3498db, #2980b9);
            border: none;
            border-radius: 22px;
            font-size: 1.15rem;
            font-weight: 700;
            color: #fff;
            cursor: pointer;
            box-shadow: 0 6px 15px rgba(52, 152, 219, 0.4);
            transition: background 0.3s ease;
            font-family: 'Arial', sans-serif;
        }

        button:hover,
        button:focus {
            background: linear-gradient(45deg, #2980b9, #3498db);
        }

        .help-links {
            margin-top: 18px;
            font-size: 0.9rem;
            color: #7f8c8d;
            text-align: right;
        }

        .help-links a {
            color: #3498db;
            text-decoration: none;
            font-weight: 600;
            font-family: 'Arial', sans-serif;
        }

        .help-links a:hover,
        .help-links a:focus {
            text-decoration: underline;
        }

        @media (max-width: 400px) {
            h1 {
                font-size: 1.8rem;
            }

            input[type="email"] {
                font-size: 0.95rem;
                padding: 12px 15px;
            }

            button {
                font-size: 1rem;
            }
        }
    </style>
</head>
<body>
    <main class="login-wrapper" role="main" aria-label="Quên mật khẩu">
        <div class="logo-container">
        </div>
        <div class="decor-circle one" aria-hidden="true"></div>
        <div class="decor-circle two" aria-hidden="true"></div>
        <h1>Quên Mật Khẩu</h1>
        <p>Nhập email của bạn để nhận liên kết đặt lại mật khẩu.</p>
        <% if (message) { %>
            <p class="<%= messageType === 'error' ? 'message-error' : 'message-success' %>">
                <%= message %>
            </p>
        <% } %>
        <form action="quen-mat-khau/gui-yeu-cau" method="POST">
            <label for="email">Email người dùng</label>
            <input type="email" name="email" placeholder="Nhập email của bạn" autocomplete="username" required />
            <button type="submit" aria-label="Gửi yêu cầu đặt lại mật khẩu">Gửi yêu cầu</button>
        </form>
        <nav class="help-links" aria-label="Liên kết hỗ trợ">
            <a href="/" tabindex="0">Quay lại đăng nhập</a>
        </nav>
    </main>
</body>
</html>